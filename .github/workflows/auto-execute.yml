name: Auto Execute Payroll

on:
  schedule:
    - cron: '0 */6 * * *'  # 6時間ごと
  workflow_dispatch:

jobs:
  execute:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      # 依存関係のインストール
      - run: npm init -y && npm install viem @circle-fin/developer-controlled-wallets dotenv --legacy-peer-deps

      # 診断用ステップ（エラー切り分け用）
      - name: Run diagnostic check
        env:
          CIRCLE_API_KEY: ${{ secrets.CIRCLE_API_KEY }}
          CIRCLE_ENTITY_SECRET: ${{ secrets.CIRCLE_ENTITY_SECRET }}
          CIRCLE_WALLET_ID: ${{ secrets.CIRCLE_WALLET_ID }}
        run: node check-wallet.mjs

      # 本番実行ステップ
      - name: Run auto-execute
        env:
          CIRCLE_API_KEY: ${{ secrets.CIRCLE_API_KEY }}
          CIRCLE_ENTITY_SECRET: ${{ secrets.CIRCLE_ENTITY_SECRET }}
          CIRCLE_WALLET_ID: ${{ secrets.CIRCLE_WALLET_ID }}
        run: node auto-execute.mjs
